# TVA Driver Story

Full-screen, Instagram-Story-style page shown to CDL drivers right after they apply.
Vertical video + burned-in captions + one tappable CTA to the application (IntelliApp).

Sent as a link via SMS. Intended custom domain: `drive.tvalogistics.com`.

## Swap before/when going live
- `truck.mp4` — replace with the real UGC creator clip (vertical 9:16).
- CTA `href` in `index.html` (search "CHANGE THIS") — point to your IntelliApp link.
- Captions array in `index.html` — match what the creator says.
